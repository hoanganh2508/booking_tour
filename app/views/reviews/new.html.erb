<div class="col-6 offset-3">
  <%= form_for [@tour, @review] do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= f.label :content %>
    <%= f.text_area :content , class: 'form-control' %>

    <%= f.submit "Tạo mới review", class: "btn btn-primary" %>
  <% end %>
</div>
