<%= link_to tour_user_like_tour_path(tour_id: tour.id, id: current_user.user_like_tours.find_by(tour_id: tour.id).id), method: :delete, data: { confirm: 'Are you sure?' }, remote: true do %>
  <i class="fa-solid fa-heart mt-1"></i>
  Bỏ thích
<% end %>
